convert_xml_to_inc.py

is a python script that takes a .xml file, reads it completely and 
converts it into an .inc file. ".inc file" is a geometry file(meshed) 
that can be generated with Abaqus or ANSA. The script reads all 
attributes of xml file and convert it into out a .inc file